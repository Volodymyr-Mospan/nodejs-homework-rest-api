const {
  validateAddBody,
  validateUpdateBody,
  validateUpdateFavorite,
} = require("./validateBody");
const isValidId = require("./isValidId");

module.exports = {
  validateAddBody,
  validateUpdateBody,
  validateUpdateFavorite,
  isValidId,
};
